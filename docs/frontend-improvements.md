# Frontend improvement and rollback ledger

This ledger supplements `visual-parity.md`. Entries are intentionally bounded so an
improvement can be reverted without changing imported content or the Payload data model.

| ID | Class | Change | Implementation hook | Rollback |
| --- | --- | --- | --- | --- |
| FE-001 | Architecture | Use one semantic token source for Tailwind, shadcn, and parity CSS. | `src/styles/tokens.css` and compatibility aliases | Restore the former variables in `globals.css` and parity roots. |
| FE-002 | Architecture | Use Font Awesome for interface icons with a typed, bounded registry. | `components/icons` and approved Payload icon options | Restore the previous icon component behind the same semantic keys. |
| FE-003 | Accessibility | Standardize focus, disabled, loading, error, touch-target, and reduced-motion behavior in primitives. | `components/ui` | Revert the individual primitive; content and adapters are unchanged. |
| FE-004 | Maintainability | Split Payload adapters from reusable site presentations. | `components/blocks` and `components/site` | Repoint the dispatcher to the former renderer during rollback. |
| FE-005 | Verification | Add a development-only design-system gallery and deterministic component fixtures. | `/design-system` development route and Playwright tests | Remove the route/tests; no production route or CMS data changes. |
| FE-006 | Performance | Keep server components as the default and isolate client state to navigation, filters, disclosures requiring state, and charts. | server/client component boundaries | Restore the previous component boundary if runtime evidence regresses. |
| FE-007 | UX | Apply Material-derived hierarchy/state/motion principles without adopting Material components or styling. | tokens and primitive state recipes | Revert the isolated token or variant. |
| FE-008 | Tooling | Normalize Tailwind class lists after component extraction. | Tailwind canonicalizer plus formatter | Revert the mechanical class-only change. |
| FE-009 | CMS integrity | Hide retained migration fields that do not affect the active frontend so editors cannot select no-op options. | Field-level `admin.hidden` flags; stored values and importer mappings are retained | Remove the flags when the matching runtime behavior is implemented and tested. |
| FE-010 | Performance | Render shared images on the server, isolate video motion handling, use composition-specific `sizes`, and default optimized images to quality 82. | `TrayportMedia`, `TrayportVideo.client`, and supported Next image qualities | Select quality 90/100 per composition or restore the former media boundary without changing CMS media. |
| FE-011 | Performance | Query indexes and complete listing libraries into compact serializable view models and persist only published route/index/listing reads. Authenticated drafts branch outside every persistent cache. | `src/data/contentIndexes.server.ts`, `src/data/listingContent.server.ts`, and `contentRoute.loader.server.ts` | Remove the `select`/view-model boundary and published cache wrappers; draft behavior and content schemas are unchanged. |
| FE-012 | Maintainability | Remove unused starter link, pagination, page-range, and legacy generic-video components after all references were proven absent. | Deleted source modules; current runtime imports `AppLink`, typed registries, and `TrayportVideo` directly | Restore the isolated modules from version control if a future route proves a requirement. |
| FE-013 | Operations | Start the production-mode local site through Next.js' generated standalone server and stage its static/public assets first, matching the configured deployment output. | `scripts/start-standalone.mjs` and the `start` package script | Restore `next start`; the build output and application source are unchanged. |
| FE-014 | Resilience | Provide branded App Router loading and recoverable error boundaries while keeping one shared designed not-found presentation for route boundaries and the status-preserving global 404. | Frontend route-group `loading.tsx`, `error.tsx`, `not-found.tsx`, global not-found route, and `NotFoundView` | Remove the boundaries/global route and restore the prior not-found markup; content routes and CMS data are unchanged. |
| FE-015 | Maintainability | Complete the Tailwind 4 CSS-first migration by moving the sole typography extension into `globals.css` and removing the legacy JavaScript `@config` bridge. | `.trayport-prose`, `components.json`, and removal of `tailwind.config.mjs` | Restore the config file and `@config` directive; rich-text content and component APIs are unchanged. |
| FE-016 | Performance | Remove the completed media compatibility alias and use Next.js 16's `preload` image prop throughout the public renderer. | `TrayportMedia.preload` and its media UI contract | Restore the local `priority`-to-`preload` adapter if an older caller is reintroduced; Payload media data is unchanged. |
| FE-017 | Maintainability | Normalize Footer globals into a Payload-independent model and focused brand, navigation, supporting, and legal presentation pieces. | Thin `Footer/Component.tsx` server adapter and `FooterPresentation` family | Repoint `Footer/Component.tsx` to the former monolithic markup; imported global fields remain intact. |
| FE-018 | Privacy/UX bridge | Replace ambiguous “Close” persistence with distinct aggregate Accept/Reject decisions, one-year expiry, cross-tab synchronization, and a stable document event/data attribute. No consent categories are invented. This remains the local bridge until the live CookieYes configuration is cloned through its Next.js integration. | `Footer/cookieConsent.ts` and `CookieNotice.tsx` | Restore the former notice client/storage key, or remove the bridge when CookieYes owns equivalent tested behavior; CMS notice content and footer data are unchanged. |
| FE-019 | Deployment security | Install the licensed Font Awesome kit in container builds through the committed scoped registry definitions and a required, instruction-scoped BuildKit secret. | `.npmrc`, Docker dependency stage, deployment contract test, and exact build documentation | Restore the former dependency instruction and supply registry authentication through another secret-only CI mechanism; application icons and content are unchanged. |
| FE-020 | Deployment integrity | Resolve media storage through explicit validated modes, reject partial S3 configuration, and require durable production storage while retaining local MinIO and a build-only mode. | `src/config/mediaStorage.ts`, Payload S3 adapter, Media `staticDir`, runtime environment contract, and focused tests | Restore the former S3 enablement predicate and fixed local path; media records and imported binaries are unchanged. |
| FE-021 | Performance | Stop embedding the same generic base64 blur placeholder in every responsive image; visible images keep intrinsic dimensions and browser-native loading while avoiding repeated HTML payload. | `components/Media/ImageMedia` and the media UI contract | Restore the fixed placeholder and `blurDataURL`; Payload media records and source assets are unchanged. |
| FE-022 | Performance | Cache one bounded snapshot of all published route claims and redirect dispositions for five minutes, rather than creating a cache entry for every attacker-controlled path miss. Dedicated redirect and route-registry tags invalidate the snapshot while exact 301/302 behavior is retained. | `src/proxy.ts`, `redirects`/`route-registry` cache tags, and proxy routing contract | Remove the snapshot cache and query published route status per request; redirect records and route semantics are unchanged. |
| FE-023 | Accessibility | Keep feature carousels user-controlled with native swipe/scroll, labelled Font Awesome controls, live position text, visible-slide focus gating, reduced-motion scrolling, and no autoplay. | Owned `FeatureCarousel.client.tsx` presentation boundary | Replace the carousel presentation with the same ordered static feature grid; imported items and their order are unchanged. |
| FE-024 | Performance/Accessibility | Keep managed videos poster-first: defer the video source until the explicit central play action, then expose native controls. Background-video reduced-motion behavior remains separate. | `TrayportVideo.client.tsx` and imported `Media.poster` relationship | Attach the source immediately and restore native controls before activation; the video and poster media records are unchanged. |
| FE-025 | Performance/Deployment | Enable Next.js Cache Components with database-free semantic partial-prerender fallbacks. Persist only published route, global, and index reads behind tagged `use cache` lifetimes; authenticated drafts always branch outside the persistent cache. | `next.config.ts`, close frontend Suspense/request boundaries, the split `contentRoute` loader/renderer, `getGlobals.ts`, and `contentIndexes.server.ts` | Disable `cacheComponents`, remove the request/static-fallback boundaries, and restore request-time published loaders; Payload data, tags, redirects, and draft access rules are unchanged. |
| FE-026 | UX | Show curated featured articles once rather than repeating them in the default index list. Filtering still searches the complete article set, so featured records remain discoverable. | Featured-ID exclusion inside `ArticleListingClient` | Remove the featured-ID exclusion; imported feature flags and ordering remain unchanged. |
| FE-027 | Accessibility/Security | Make disabled button-links inert even when a caller supplied click or tab props, and preserve caller relationship tokens while adding both new-tab protections. Header links use the same complete protection pair. | `components/ui/button-link.tsx` and Header link props | Restore the former prop forwarding and `noreferrer`-only Header values; link destinations and CMS values are unchanged. |
| FE-028 | Resilience/Performance | Reject incomplete or non-positive intrinsic dimensions before a Payload string source reaches `next/image`; static imports and intentional fill images remain supported. | `components/Media/ImageMedia` | Remove the dimension guard; media records and source assets are unchanged. |
| FE-029 | SEO/Maintainability | Express the favicon set through the App Router metadata API and retain the site Open Graph description when a document has no specific description. | Frontend root `metadata` and `generateMeta` | Restore the manual root `<head>` links and empty per-document Open Graph description; CMS SEO values are unchanged. |
| FE-030 | Maintainability | Remove unused starter-era document/user/redirect fetchers, redirect renderer, and clickable-card hook after repository-wide reference checks. The route registry, Proxy, Payload auth, and native semantic links remain authoritative. | Deleted `PayloadRedirects`, `getDocument`, `getRedirects`, `getMeUser`, and `useClickableCard` modules | Restore the isolated files from version control if a verified consumer is introduced. |
| FE-031 | Performance/Accessibility | Attach desktop navigation's document-level Escape and outside-pointer listeners only while a dropdown is open, and close an open dropdown when keyboard focus leaves the navigation. | `Header/DesktopNavigation.client.tsx` | Remove the open-state early return and navigation blur handler; navigation markup and CMS values are unchanged. |
| FE-032 | Deployment/SEO | Resolve and validate one exact public HTTP(S) origin for metadata, Payload CORS, Next image allowlisting, previews, and sitemap output; explicit configuration wins over platform discovery. | `config/publicOrigin.ts`, `getURL.ts`, `next.config.ts`, and `next-sitemap.config.cjs` | Restore the former per-consumer environment resolution; content, routes, and deployment secrets are unchanged. |
| FE-033 | Performance | Cache the published content-sitemap data for five minutes behind its existing routable-content invalidation tag while retaining a request-time route boundary and an uncached exported query for integration verification. | `content-sitemap.xml/route.ts`, `data/contentSitemap.server.ts`, and the `content-sitemap` tag | Call `getContentSitemap` directly from the route handler; route claims and indexing rules are unchanged. |
| FE-034 | Performance | Advertise column and market-map background images at their actual content-column width instead of `100vw`, reducing oversized responsive image selection without changing the fill layout. | `ContentSectionBlockAdapter`, `MarketCoverageComponentAdapter`, and `TrayportMedia`'s `content` composition | Remove the two explicit `composition="content"` props; managed media and rendered geometry are unchanged. |
| FE-035 | CMS integrity | Expose bounded component-type classes on content sections and columns so styling follows semantic content when editors reorder sections instead of relying on document position. | `ContentSectionPresentation` classes plus Home parity selectors | Restore the prior ordinal Home selectors; Payload fields and imported section order are unchanged. |
| FE-036 | Visual parity/Performance | Self-host deterministic normal and italic subsets of the exact Inter 4.1 variable fonts plus the regular/bold static faces used by the WordPress chart runtime, correcting glyph metrics without retaining its third-party font request. The checked-in source/output hashes and OFL make the result reproducible and auditable. | `src/styles/fonts.css`, hash-locked faces under `src/styles/fonts`, and the `--font-inter` token | Restore the pinned Fontsource import or the former `next/font/local` face; content and typography tokens are unchanged. |
| FE-037 | Accessibility | Preserve 48 px narrow-screen EEX market links and a 44 px Contact action while the structured route adopts the reference's otherwise smaller tile recipe. | Mobile rules in `parity-structured.css` | Remove the scoped minimum heights; imported relationships, order, and link destinations are unchanged. |
| FE-038 | Resilience/Security/Performance | Resolve public route status before streamed rendering, use a designed global 404 with a real 404 status, separate reserved CMS namespaces from implemented proxy bypasses, and admit draft-only routes only through a signed, short-lived, path-bound preview token. Runtime redirects reject malformed, self-referential, chained, or reserved same-origin destinations. | `src/proxy.ts`, `routing/reservedPaths.ts`, `routing/previewAccess.ts`, and the global not-found boundary | Restore route-local `notFound()` as the sole status mechanism and the former preview cookie behavior; route records and editor workflow are unchanged. |
| FE-039 | CMS integrity/Security | Apply one managed destination policy at Payload write boundaries and fail closed again at presentation boundaries. Internal paths are canonical, external links are HTTPS-only without credentials, relationship links require a published path, and external media is allowlisted to reviewed Trayport CDN image/video paths. | `routing/urlPolicy.ts`, link/media fields, redirect validation, and public adapters | Remove the shared validators/normalizers and restore each field/adapter's previous URL handling; stored content is unchanged and must be re-audited before rollback. |
| FE-040 | CMS integrity/UX/Accessibility | Remove the unused standalone `primaryAction` control that duplicated Contact Us, and expose the three existing CMS utility links consistently in the portal-owned mobile menu with their bounded Font Awesome icons. Portal-owned selectors also retain dark submenu text and compliant contrast on the white panel. | Navigation global/import adapter, `MobileNavigationPanel`, and portal-scoped shell styles | Restore the standalone field/model/import mapping and remove the mobile utility list/styles; desktop dropdown utilities and imported link destinations are unchanged. |
| FE-041 | Performance/CMS integrity | Invalidate public route, index, sitemap, global, and layout caches only when a write can change the published projection. Autosaves and draft-only saves remain private; publish, unpublish, path move, delete, and explicit revalidation-disable behavior remain intact. | `hooks/publicProjection.ts`, content/global revalidation hooks, and route-mutation lifecycle intent | Restore unconditional after-change invalidation; draft data, published data, and route ownership are unchanged. |
| FE-042 | Accessibility/CMS integrity | Enforce a mutually exclusive meaningful-image or decorative-image state for uploaded images, while defensively rendering editor-marked decorative assets with empty alternative text. Video and document metadata remain outside this image-only rule. | Media `alt` validation and `ImageMedia` presentation boundary | Remove the conditional validator and decorative rendering override; existing media records are unchanged. |
| FE-043 | Accessibility | Generate instance-unique chart heading and market-map title/description IDs so repeated managed blocks keep valid ARIA relationships. | `useId()` within the two specialist presentations | Restore the fixed/model-only IDs; block data and visible output are unchanged. |
| FE-044 | Deployment integrity | Require an explicit non-local HTTPS public origin before a production container build can bake metadata, sitemap, image, preview, and browser configuration. Local development retains its existing fallback outside the container release path. | Docker build argument plus `scripts/assert-production-origin.mjs` | Restore the Docker argument default; runtime origin configuration and content are unchanged. |
| FE-045 | Resilience/Accessibility | Keep the designed global 404 status-preserving rewrite on its canonical trailing-slash target, allow Next's internal not-found RSC payload to render, and reserve the `main` landmark for the route-level loading state rather than nested Suspense fallbacks. This prevents a production-only 308/RSC retry loop and duplicate landmarks under partial prerendering. | `src/proxy.ts`, route loading state, catch-all/content Suspense fallbacks, and production route checks | Restore the slashless rewrite/direct empty response and nested loading landmarks; content records and route ownership are unchanged. |
| FE-046 | Verification | Refresh only the owned deterministic design-system gallery baselines after the accepted Inter 4.1 metrics and new component/accessibility states changed the complete gallery by a stable 3%. WordPress golden-route references remain immutable. | Two PNGs under `tests/visual/gallery`; gallery Playwright project | Restore the prior gallery PNGs; production code, WordPress reference captures, and visual thresholds are unchanged. |
| FE-047 | Performance/CMS integrity | Invalidate cached content by canonical route, listing family, venue index, market index, sitemap, route registry, or redirect projection. A same-path public edit refreshes its route and sitemap metadata without flushing the proxy route-status snapshot; path moves, publish/unpublish, and routed deletes refresh both. | `src/data/cacheTags.ts`, content/redirect/global hooks, and cache-contract tests | Recombine sitemap and route-registry invalidation behind one broad tag; stored content and publication workflow are unchanged. |
| FE-048 | Resilience/Performance | Stream article, learning-video, market-coverage, and market-data blocks behind close accessible Suspense fallbacks, and distinguish a valid empty market dataset from a PostgreSQL outage. | Typed block registry, market-data result model, and chart presentation state | Remove the four close Suspense boundaries and collapse `unavailable` back into the empty state; chart configuration and market facts are unchanged. |
| FE-049 | Performance/Accessibility | Keep background-video sources detached until the browser confirms that reduced motion and data saver are both off. Poster-backed foreground video remains explicitly user activated. | `TrayportVideo.client.tsx` and media UI tests | Restore eager background source mounting/autoplay; media records and poster relationships are unchanged. |
| FE-050 | Maintainability | Put document resets in Tailwind's base layer, keep reusable action/eyebrow/loading recipes in the component layer, reject duplicate token sources, and delete CSS selectors whose former React owners no longer exist. | `globals.css`, token contract, and selector ownership audit | Restore the isolated removed rules and prior layer placement; current markup and semantic tokens are unchanged. |
| FE-051 | Deployment integrity | Run committed Payload migrations in a dedicated image before a stop-first single-process first release, verify the exact migration manifest, and expose separate process liveness and database/schema readiness probes. | Docker `migrator`/`runner` targets, health routes, deployment scripts/manifest, and `docs/deployment.md` | Return to the former web-only image and manual migration command only together with an equivalent externally verified release procedure. |
| FE-052 | Resilience/Security | Add a self-contained root-layout error boundary that reports only an opaque digest, plus safe response headers for transport, MIME, referrer, permissions, and same-origin framing. CSP remains a documented report-only/nonce gate because a naïve policy breaks the current Next.js shell. | `global-error.tsx`, `GlobalErrorFallback`, safe error reporter, security-header config, and runtime contracts | Remove the boundary/header config and rely on platform defaults; content routes and Payload data are unchanged. |
| FE-053 | Verification | Exercise all 81 semantic Font Awesome registry keys and real Chromium keyboard/focus flows for Dialog, Popover, Dropdown Menu, Select, Checkbox, and Textarea, including computed cascade ownership. | Guarded gallery plus gallery/interaction/UI tests | Remove the exhaustive hidden registry mount and added state tests; production components and routes are unchanged. |
| FE-054 | Performance/CMS integrity | Tag each cached public projection with the exact populated records it consumes while within the slots left after all primary owner tags are reserved, then switch the final combined set to collection-scoped tags rather than truncating it. Dependency hooks invalidate both forms, so multi-source and large projections broaden refreshes without becoming stale; draft autosaves remain private. | `src/data/cacheDependencies.ts`, document/collection-scoped cache tags, collection hooks, and cache-contract tests | Remove relationship tag collection and dependency hooks; the five-minute cache lifetime still bounds staleness and stored relationships are unchanged. |
| FE-055 | Resilience/Performance | Bound PostgreSQL connection, statement, and query waits for both market-data blocks and the every-route proxy preflight. Market data exposes an unavailable state; proxy failures continue to surface as infrastructure errors rather than false 404s. | Pool timeout contracts in `src/data/market-data/loadMarketData.server.ts` and `proxy.ts` | Remove the timeout options and rely on driver defaults; SQL, cache keys, and failure semantics are unchanged. |
| FE-056 | Security/Resilience | Report only the owning boundary and a validated opaque Next.js digest from both client error conventions. Messages and stacks can contain credentials, request data, or private content and never cross this reporting boundary. | `utilities/reportClientError.ts`, frontend `error.tsx`, `global-error.tsx`, and runtime contracts | Restore direct console reporting only after an equivalent structured redaction boundary exists; fallback UI and retry behavior are unchanged. |
| FE-057 | Maintainability | Keep reusable and compatibility block recipes in Tailwind's component layer so merged caller utilities retain the documented override contract, including on Hero. Exact route layers remain separately reviewable. | `globals.css`, layered `parity-blocks.css`, and computed gallery cascade tests | Remove the parity layer wrapper to restore author-CSS precedence; component data and markup are unchanged. |
| FE-058 | Visual parity/Accessibility | Preserve WordPress's distinct semantic leading action glyphs, including Forward, while link-style actions consistently end in a chevron. New-tab destinations add a screen-reader-only warning and retain both relationship protections. | Action icon schema/import migration, Font Awesome registry, `ActionLink`, and importer/UI contracts | Map Forward back to Arrow Right and restore destination-based trailing glyph selection; labels, destinations, and CMS records are unchanged. |
| FE-059 | Resilience | Remove a failed Highcharts runtime script and clear its rejected singleton promise so a later chart mount can retry within the same browser session instead of requiring a full reload. | `MarketVolumeChartRuntime.client.tsx` and its remount/retry UI contract | Restore permanent rejection caching; chart data and the table fallback are unchanged. |
| FE-060 | Migration/UX | Keep every accepted navigation/footer destination usable during incremental migration: the 27 accepted roots, five published-banner Page dependencies, and two virtual indexes stay internal, while exactly 30 plan-only same-site destinations use canonical live-site HTTPS fallbacks. The count includes 25 leaf destinations plus the clickable Company, Products, Markets, Regions, and Resources section roots. Grouped navigation still marks the current root from an owned descendant even when the root landing page is a fallback. | Import URL policy, transformed Navigation/Footer globals, and shared header current-path helper | Change one fallback back to its canonical internal path when its content route enters the accepted slice; root-current detection can return to root-link-only without changing content or destinations. |
| FE-061 | CMS integrity/UX | Treat the dormant Request A Demo WordPress page as an identity-preserving temporary 302 to Contact instead of importing its inactive HubSpot presentation. The real conversion-page publication guard remains in force and active HubSpot identifiers elsewhere remain launch scope. | Pilot scope disposition plus managed Redirect from `/request-a-demo/` to `/contact/` | Remove the redirect and migrate WordPress page 4031 as a reviewed `page.conversion` only after the bounded HubSpot integration is approved. |
| FE-062 | CMS integrity | Import Contact as standard managed content while omitting its two unused HubSpot form blocks and dead prompt copy; retain the support email/phone and all four office references without treating this route-specific source fact as a global HubSpot exclusion. | Contact transform policy and acceptance validation | Re-enable a bounded HubSpot component and its prompt only after selecting and testing an active form identifier; the untouched WordPress source remains the recovery reference. |
| FE-063 | UX/Accessibility | Render Market Matrix from authoritative Venue relationships with exactly one managed block and retain an accessible data view alongside the visual matrix. | `marketMatrix` block, loader, importer reconciliation and page 2231 transform | Remove the block from page 2231 to return to imported static content, or restore its isolated renderer; Venue relationship data remains intact. |
| FE-064 | Performance/Architecture | Bound always-hydrated controls to four shell and six primitive Font Awesome definitions rendered through a small owned SVG boundary, while retaining the official React renderer for CMS-selected icons on the server and passing those icons into client components as rendered slots. Production builds enforce a 60 KiB gzip budget over the complete frontend-layout entry and reject representative CMS icon definitions. | `ShellIcon`, `ControlIcon`, `BoundedIcon`, server presentation adapters, and postbuild public-shell budget | Point client controls back to `AppIcon`, remove the server slot adapters and budget gate, and retain the unchanged semantic CMS icon names. |
| FE-065 | CMS/data integrity | Require each Data Chart to relate to a managed imported Asset Class, derive the application market-data key from that relationship, and reject incomplete or backwards quarter ranges at publication. | `DataChartComponent`, archetype publication validation, typed server adapter, and import relationship mapping | Restore the hidden numeric lookup as the runtime authority and remove the range guard; market facts and immutable source extracts are unchanged but chart ownership must be re-audited. |
| FE-066 | Migration fidelity | Preserve imported H2-H4 headings, ordered/unordered lists, and nested list structure as native Lexical nodes, and wrap full-article components with the current bounded Content Section fields instead of obsolete generic theme/spacing keys. | Lexical transform and `mapArticleLayout` | Restore the previous flattening/wrapper mapping and retransplant from the immutable source run; Payload-native content and source HTML are unchanged. |
| FE-067 | Content integrity | Always render an authored Venue description, including when its copy matches the SEO description. SEO metadata is not a body-content deduplication key. | `VenueView` structured About section | Restore the former comparison guard; Venue records and SEO metadata are unchanged. |
| FE-068 | Visual stability | Keep public rich text on the approved light theme when the operating system prefers dark colors, instead of applying the typography plugin's dark inversion without a complete dark design system. | Root light `color-scheme`, `RichText`, and dark-preference route contract | Reintroduce a dark prose variant only alongside approved semantic dark tokens and complete route-level visual evidence; content is unchanged. |
| FE-069 | CMS integrity/Resilience | Apply image-only, video-only, or image-or-video relationship factories to every visual CMS field and fail closed when presentation receives a document, missing MIME metadata, an unsupported type, or a missing record. The Media library can still retain PDFs for explicit document workflows. | Shared media-upload field factories, server validation, and `TrayportMedia` | Restore per-field upload definitions and permissive rendering only after each visual consumer has an equivalent MIME guard; stored Media records and valid relationships are unchanged. |
| FE-070 | CMS/data fidelity | Preserve the six active WordPress chart semantics with one bounded contract: execution-type volume stacked columns or Hub-based volume columns/price lines; month/quarter/year grouping; managed disjoint Hub filters; and explicit available, empty, unavailable, and unsupported states. Queries are capped at 40 matching periods. | Data Chart schema/import validation, managed Asset Class/Hub relationships, market-data loader, typed presentation, and migration `20260804_113221_data_chart_semantics` | Restore the prior execution-volume-only adapter and hidden controls, then re-import the immutable WordPress source; application facts and retained migration keys remain unchanged, but the four Hub-series charts would need an explicit fallback or removal. |
| FE-071 | Migration integrity | Canonicalize every imported managed slug through the same lowercase, transliteration, and hyphen policy used by authored records. This removes underscore-only source drift such as `iron_ore` without changing legacy IDs or paths. | Shared `normalizeTrayportSlug` field/import boundary and source-validation contract | Restore raw source slugs and rerun the immutable import; route claims and legacy IDs must then be revalidated for collisions and repeat-load churn. |
| FE-072 | CMS fidelity | Allow at most eight columns in a Content Section, the smallest bounded limit that can reproduce the accepted seven-column Asia-Pacific source section while preserving the existing responsive grid. | `ContentSection.columns.maxRows` and editor-control contract | Return the limit to four only after splitting the accepted seven-column section into a reviewed replacement composition; imported source evidence is unchanged. |
| FE-073 | Visual stability | Keep rich-text variable defaults in Tailwind's utility layer so they win the typography plugin's compiled defaults, but inherit the surrounding semantic foreground through `currentColor`. Own the default statistic-label colour in the component layer so more-specific dark-section rules can invert it. This fixes dark-OS prose drift, preserves dark-surface composition, and retains secondary label hierarchy without adding a theme. | `globals.css` typography-variable and statistic-label component rules, `StatGrid`, computed dark-composition coverage, dark-preference E2E, and deterministic gallery | Restore root-pinned prose variables or a label utility only alongside equivalent compiled-cascade coverage for both light and dark contexts. |
| FE-074 | Build resilience/Security | Resolve and reject preview URL, secret, and path input before asynchronous Payload initialization. This makes the request-bound route deterministically dynamic during Next's production analysis and avoids starting CMS work for malformed or unauthorized previews. | `/next/preview` request boundary and consecutive production-build verification | Move Payload initialization above request parsing only if the framework's prerender classification remains deterministic and equivalent unauthorized-request cost is demonstrated. |
| FE-075 | Architecture/Accessibility | Derive carousel state from the CSS-owned visible-item contract so status text, inert slides, controls, and route-specific two-up geometry cannot disagree at a breakpoint. Replace native FAQ pseudo-text state glyphs with bounded Font Awesome plus/minus icons while retaining native `details` keyboard behavior. | `FeatureCarousel`, the `--trayport-carousel-visible-items` component contract, FAQ presentation, semantic icon registry, and focused UI/browser checks | Restore JavaScript-owned breakpoint counts or CSS pseudo-glyphs only with equivalent extra-wide geometry, assistive-state, and icon-system evidence. |
| FE-076 | Architecture/Design system | Make URL-backed article search correct in server output and reset local filter state when the query changes; use stable Payload IDs for rendered block keys; keep route-loading landmarks available; route internal rich-text links through Next navigation and expose every routable collection in the managed picker; emit article-specific social metadata; remove the global `.container` collision; wire typography, section-spacing, and layer tokens to their owners; distinguish filled secondary buttons from outlines; and make the Hero Image appearance the sole CMS control that displays hero media. | Content route and listing adapter, typed block registry, loading fallbacks, Rich Text/editor configuration, metadata adapter, semantic layout primitives/tokens, button variants, Hero adapter, and focused contracts | Each change is independently reversible at its named boundary. Restore prior behavior only with equivalent server-response, live-preview identity, landmark, navigation, managed-link, metadata, cascade, primitive-gallery, and editor-control evidence. |
| FE-077 | Performance/Architecture | Keep draft-only and optional listing, carousel, matrix, chart, and video implementations behind client-owned, SSR-preserving dynamic boundaries. Protect the measured reduction with explicit route-pattern JavaScript/CSS/HTML/RSC budgets and a durable postbuild report. | Dynamic client wrappers, public-shell budget, route-budget configuration/checker, and production build evidence | Restore a static implementation import or raise a budget only with a documented route-level measurement; the server presentations and CMS data contracts are unchanged. |
| FE-078 | Architecture/Performance | Remove the unused legacy Content Views re-export and the unreferenced 319 KiB predecessor font after proving all runtime consumers use the owned content modules and a single self-hosted Inter entrypoint. | Content-view ownership contract and frontend font import | Restore either artifact from version control only if a real tooling or runtime consumer is identified; public content and font configuration are unchanged. |
| FE-079 | Architecture | Split the public route facade into explicit path, type, cached-loader, renderer, and metadata owners; restrict each route-owner read to a generated-type-backed top-level projection while retaining relationship depth; move PostgreSQL market-data access from the component tree into a server-only data module with client-safe types; and remove its unused quarterly compatibility API. | `contentRoute.*`, `src/data/contentRouteProjection.ts`, `src/data/market-data/*`, the removed-facade contract, and focused cache/data contracts | Recombine the modules, remove the projections, or restore the compatibility API only for a demonstrated consumer; route resolution, cache keys, relationship depth, queries, imported facts, and presentation contracts are unchanged. |
| FE-080 | Accessibility/Resilience | Keep streamed route fallbacks as non-landmark status regions so partial-prerender replacement cannot temporarily create duplicate `main#main-content` landmarks. Keep lazy listing controls disabled and explicitly busy until their client implementation hydrates, preventing an early user interaction from being silently lost. | Frontend `ContentLoadingState`, article and learning-video listing clients, route browser coverage, and server-markup search coverage | Restore landmark fallbacks or immediately enabled lazy controls only with browser evidence that IDs remain unique and pre-hydration interactions are replayed reliably. Content and filter semantics are unchanged. |
| FE-081 | Performance/Verification | Record LCP, CLS, FCP, TTFB, load timing, navigation bytes, and resource transfer summaries for every desktop/mobile golden route from the production build. Treat local timings as comparable evidence rather than brittle CI thresholds, alongside the blocking build-artifact route budgets and optional-module network checks. | `tests/e2e/performance-vitals.e2e.spec.ts`, `performance-boundaries.e2e.spec.ts`, and generated `output/performance/web-vitals-current.json` evidence | Remove the evidence probe and generated artifact; no runtime code, CMS data, or build budget changes are required. |
| FE-082 | Performance/Resilience | Keep compact public media projections while retaining `filename`, the metadata required by the S3 adapter to derive local media URLs. This restores imported local artwork without widening route reads to complete Media documents. | Listing and content-index media selects plus the compact-loader contract | Remove `filename` only if the storage adapter can derive a stable public URL from another selected field; Media records and binaries are unchanged. |
| FE-083 | Verification | Make strict visual mode depend on the configured reference host, keep proxying as request rewriting only, require the canonical candidate command to target an existing production server, wait for both Home Highcharts plots, and keep generated reports outside ESLint's source graph. | Visual Playwright config/spec, production/dev scripts, `output/**` ESLint ignore, and the semantic harness contract | Restore the prior dev-starting command and proxy-based mode only together with equivalent production/reference discovery, chart-settle, and visual-to-lint evidence. |
| FE-084 | Visual parity/Performance | Create Highcharts as soon as its lazy same-origin runtime is ready, without gating construction on the static Inter request. This deliberately preserves the reference's fallback-font measurement followed by an exact self-hosted Inter swap, matching plot margins, legend wrapping, and automatic quarter labels while removing an avoidable render gate. The reference HTML tooltip format shows the period total followed by ungrouped series values; managed text is escaped before it enters the HTML tooltip. | `MarketVolumeChartRuntime.client.tsx` runtime/tooltip boundary, exact static faces, and focused UI coverage | Restore the `loadChartFont` readiness gate and the former native-SVG tooltip options; chart data, runtime isolation, and fallback table are unchanged. |
| FE-085 | Migration fidelity/Resilience | Recover only explicitly approved unavailable WordPress images from one audited HTTPS origin before transformation, with exact path/type/dimension checks, bounded sequential streaming, hash-pinned complete provenance, retry-safe source/manifest commits, and run-local loader verification. This restored original media `9698` and `9727` without substitutes. | `content:recover-media`, media-recovery evidence in accepted run `visual-final-parity-20260804-1835`, and focused interruption/cap/loader tests | Extract a fresh run and omit the optional recovery stage; affected records return to the explicit unavailable-media path while the previous run and binaries remain auditable. |
| FE-086 | Architecture | Enforce public CMS loaders as `server-only`, import route owners directly, remove the mixed compatibility facade, and render the route-result union through a compile-time exhaustive switch. | `contentRoute.loader.server.ts`, `contentRoute.renderer.tsx`, both frontend route entry points, `getGlobals.ts`, and focused contracts | Restore a facade only for a demonstrated consumer and retain equivalent server-boundary and exhaustiveness checks; route data, cache semantics, and presentation output are unchanged. |
| FE-087 | React/Accessibility | Model hydration availability with `useSyncExternalStore` server/client snapshots instead of mounting effects, retaining disabled and busy filter controls in server HTML without an avoidable extra effect-driven render. | Shared `useHydrated` hook and both lazy listing clients | Restore the effect guard only if React's external-store hydration contract becomes unsuitable; filter data and interaction semantics are unchanged. |
| FE-088 | Architecture/Tailwind | Keep Footer schema vocabulary in a neutral domain module shared by Payload and presentation, and rely on Tailwind's complete generated `sr-only` utility instead of maintaining a duplicate partial definition. | `config/footer.ts`, Footer schema/adapter/model imports, `globals.css`, and focused schema/cascade/accessibility contracts | Move the options back into the presentation layer or restore the local utility only with equivalent dependency direction and compiled-cascade evidence; footer content and hidden labels are unchanged. |
| FE-089 | Performance/Resilience | Reproduce the exact Home connection topology without paying a global Mapbox cost: always server-render the accessible fallback, derive 55 markers and 759 same-class lines from managed relationships, and load the pinned provider runtime/CSS only near the Home map viewport when validated deployment values exist. Pre-ready failure retains the fallback, and routes without provider-backed map blocks issue no Mapbox requests. | `connectionsMap` configuration/data modules, Home map server adapter, dynamic runtime island, importer `show_lines` preservation, and model/lifecycle/network-boundary tests | Omit provider values to retain only the fallback, or remove the isolated Mapbox runtime and dependency; imported Hub data, Payload controls, server markup, and static fallback remain unchanged. |
| FE-090 | Architecture/Visual parity | Reserve the checked-in `/brand/*` namespace at both the content-validation and route-preflight boundaries so immutable brand assets are served by Next instead of being rewritten as unknown CMS routes. | `reservedPaths.ts`, proxy/content-path contracts, the hash-guarded hero artwork, and its production HTTP check | Remove `brand` from both bounded first-segment sets only after every checked-in asset moves to another reserved namespace; CMS routes must then be collision-audited. |
| FE-091 | Visual parity/Maintainability | Keep source-measured Home and Joule finishing rules in their route stylesheets: one-pass hero artwork, normal-case badges, source stat/card/media/grid geometry, overlay carousel controls, and desktop-only FAQ metrics. Shared presentations and editor controls remain unchanged. | `parity-home.css`, `parity-joule.css`, the immutable visual harness, and the frontend-system contract | Remove the two isolated rule groups to restore the reusable design-system recipes; managed content and component APIs are unchanged. |
| FE-092 | Migration fidelity/Accessibility | Treat reusable-video names as administrative labels rather than public captions, while retaining the direct-file fallback as an out-of-flow, focus-revealable recovery link. Seal and publish that correction through a fresh immutable import run rather than altering prior evidence. | Reusable-video transform, Home media parity rules, accepted run `visual-final-parity-20260804-1835`, and focused transform coverage | Restore the caption mapping and visible fallback flow, then import through another fresh run; the source reusable and managed media relationship remain unchanged. |
| FE-093 | Accessibility/Visual parity | Retain 44 px Home content actions and native chart-data disclosures where WordPress uses smaller controls or chart-only output. Their bounded height deltas are measured separately from exact source geometry and do not expand CMS layout freedom. | Shared action primitive, `dataChart.showDataTable`, Home route rules, and VP-034 | Remove the scoped minimum action height or disable the imported disclosure flag to reproduce the smaller source footprint; content and market facts are unchanged. |
| FE-094 | Verification | Refresh only the two owned design-system gallery images after reviewing the stable final Inter/body typography output at both viewports. The immutable WordPress route references remain unchanged and continue to fail closed. | `tests/visual/gallery`, two isolated repeat captures, and the gallery visual suite | Restore the preceding two gallery images; production code, CMS data, and WordPress reference captures are unchanged. |
| FE-095 | CMS/Identity | Brand the Payload entry/dashboard and keep future TIM customer identities in a non-authenticating collection separate from CMS users, with no credential/token/session fields. Administrators and Editors retain publication rights while destructive/account/migration controls remain Administrator-only. | Payload admin components/styles, `customer-identities`, and access contracts | Remove the optional identity collection/admin branding without changing editor users; TIM integration still requires its own server-side boundary. |
| FE-096 | Map parity/Accessibility | Add the live regional connectivity model and interactions while keeping the global schematic bounded: Payload-managed Region geometry/points, Hub metadata/routes, Venue hierarchy, Asset Class appearance, period summaries, route-owned Mapbox, and an accessible fallback/list. | `marketCoverage` modes, regional map model/loader/presentation/runtime, map-data endpoint, and migration mapping | Return affected blocks to `globalConnections` or remove the regional runtime; managed map records remain available but live regional parity is then incomplete. |
| FE-097 | Matrix UX/Data integrity | Extend the Venue-authoritative Market Matrix with Hub filtering, accessible collapsible Venue groups, link fallback, safe deterministic CSV, and a formatted current-view Excel workbook. | Matrix projection/presentation/export module and Venue connection validation | Remove Excel and the additional controls while retaining the underlying Venue relationships and accessible matrix table. |
| FE-098 | Data operations | Replace legacy-ID-only chart lookup with stable managed keys/aliases and add an Administrator validate/preview/commit/history workflow that performs metric-preserving transactional fact upserts without exposing raw facts as Payload fields. | Asset Class/Hub market-data fields, `market-data-imports`, parser/service, application table migration, and chart/map loaders | Disable new imports and fall back to immutable migration input; do not restore editable raw CMS facts. |
| FE-099 | Accessibility/UX | Keep the regional market map's Region, Asset class, Data interval and Period controls in a persistent bar above the map instead of the reference's hover-opening overlay inside the map's top-left corner. A hover-only menu cannot be opened by touch or keyboard, so the reference behaviour is unreachable for those users; the controls are otherwise identical in order, options and effect. | The control bar in `DynamicRegionalMarketMap.client.tsx` | Move the control group back inside the map container behind a hover affordance; the controls, their options and the map runtime are unchanged. |

## Current slice status

- FE-001 through FE-003 and FE-005 through FE-026 are implemented and covered by focused
  contracts or browser checks.
- FE-004 has its typed registry and content-family boundaries in place. The legacy BEM
  presentation bodies remain intentionally intact until each affected golden route is migrated.
- FE-006 is implemented for route/index reads, media, content views, and the header shell. Listing
  filters, disclosures, and charts remain client components because they own real interaction.
- FE-027 through FE-094 are bounded production-code audit and migration-slice changes. The final
  implementation-slice evidence passes Prettier, canonical Tailwind, ESLint, TypeScript, 467
  integration tests across 55 files, 103 UI tests across 19 files, all four gallery checks across
  desktop and mobile, the production build and route budgets, and 239 of 258 E2E tests with 19
  documented skips.
- FE-095 through FE-098 are the current local Payload, regional-map, Matrix, and market-data
  delivery decisions. Their verification belongs to the combined slice handoff rather than the
  earlier FE-027–FE-094 evidence totals.
- Exact visual acceptance remains separate: a route is not marked visually complete merely because
  it now runs through this frontend system.

Any additional visible deviation from `trayport.local` must be added here or to
`visual-parity.md` before it is accepted.
