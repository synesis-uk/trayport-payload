server {
  listen 3090;

  location / {
    proxy_http_version 1.1;
    proxy_pass http://trayport_local_nginx:80;
    proxy_set_header Host trayport.local;
    proxy_set_header X-Forwarded-Host trayport.local;
    proxy_set_header X-Forwarded-Proto http;
  }
}
